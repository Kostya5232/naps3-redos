# HP M428/M429 sends a zero-length USB packet after almost every 8 KiB of an
# eSCL image. The NAPS3 ipp-usb build uses this exact quirk to enable its
# large receive buffer and zero-delay burst handling. Other printers retain
# the upstream behaviour.
[03f0:c32a]
  zlp-recv-hack = true
